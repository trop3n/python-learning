[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "3.12.7"
requests = "^2.32.3"
websockets = "^14.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
